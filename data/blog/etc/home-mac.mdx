---
date: "2023-06-19"
title: "컴퓨터 세팅"
tags: ["mac", "windows"]
summary: "개인용 컴퓨터 클린설치 후 세팅 목록"
---

## 설치

### 필수

- 크롬
- node
- go
- dotnet sdk [https://learn.microsoft.com/ko-kr/dotnet/core/install/macos#make-net-available-system-wide]
- dotnet [https://dotnet.microsoft.com/ko-kr/download/dotnet]
- Jetbrain Toolbox
  - Goland
  - Webstorm
  - Rider
  - Datagrip (선택)
- CleanMyMac + (bartender4 or vanilla)
- KakaoTalk
- Docker or Podman
- Chat GPT

### 선택

- Pocket
- Ridibooks
- TickTick
- Xmind
- PostMan
- Slack & Discord

## 윈도우

- Wacom
- PowerShell
- PC Manager
- git

```PowerShell
winget install Git.Git
```

## 맥

- xcode command line tools

```bash
xcode-select --install
```

- homebrew [https://brew.sh/index_ko]

- node

```bash
brew install node
```

- go

```bash
brew install go
```

- git

```bash
brew install -s git
```

- iterm2

```bash
brew install --cask iterm2
```

- Visual Studio Code [https://code.visualstudio.com/]

- etc

```bash
# tree
brew install tree
# podman
brew install podman
```

## 설정

- ssh-key

```bash
ssh-keygen
```

- ssh 로 github push 가 되지 않는 경우 .ssh 폴더에 config 파일 생성 후 아래와 같이 기입

```
Host github.com
    Hostname ssh.github.com
    Port 443
```

- 터미널 단축키 .zshrc

```
alias k="kubectl"
alias ll="ls -al"
alias tf="terraform"
```

- 맥북 대기상태 전력소모량 줄이기(ventura 부터는 사라진 것으로 보임)

```bash
sudo pmset standbydelaylow 900 standbydelayhigh 1800
# 실리콘 맥북은 기본적으로 0으로 설정되어 있는 듯 하다.
# sudo pmset standbydelaylow 0 standbydelayhigh 0
```
